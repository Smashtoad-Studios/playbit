local module = {}
playdate.display = module

function module.setOffset(x, y)
    playbit.logger.printWarning("playdate.display.setOffset() is not yet implemented.")
    -- Has no effect when not in the base graphics context
end

function module.setRefreshRate(rate)
    playbit.logger.printWarning("playdate.display.setRefreshRate() is not yet implemented.")
end